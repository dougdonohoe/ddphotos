# Default albums directory, sync directory (both relative to repo root) and site ID.
# Override via environment variable when building for a specific site.
DDPHOTOS_ALBUMS_DIR=albums
DDPHOTOS_SITE_ID=sample
DDPHOTOS_SYNC_DIR=sync
